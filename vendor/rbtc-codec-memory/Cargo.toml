[package]
name = "rbtc-codec-memory"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Safe budgeted encoding and decoder memory estimates for rBTC's pinned codec"

[dependencies]
libc = "0.2"
zstd-sys = { path = "../zstd-sys", version = "=2.1.0", default-features = false, features = ["experimental"] }
