[package]
name = "rbtc-codec-memory"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-only"
description = "Safe allocation-free memory estimates for rBTC's pinned native codec"

[dependencies]
zstd-sys = { version = "=2.1.0", default-features = false, features = ["experimental"] }
