# 审计、验收修复与存储清理，2026-09-12

## 范围和版本

工作区 Git HEAD 为 `2f77c0e`，但其 Rust 工作文件已逐文件匹配此前保存的
`649f937681a063f466ce1658873af173d7395e99` 提交链。审计保留该链，不把旧的
P0/P1 实现重新打成重复提交。新增差异主要是存储长跑 runner、公网 finalizer、
它们的测试和执行文档。机器专用的 `scripts/start-codex-rBTC.sh` 保留为本地
未跟踪工具，不作为节点或验收代码发布。

检查的生产边界包括：SCRIPT 任务所有权/取消和字节队列、PSBT 长度与精确消费、
P2P 尾部载荷和 v2 回退分类、头 DAG 暂存/持久化/回滚及 serving 投影、
交易准入的候选原子性/上下文验证/脚本承诺、费用衰减、cluster/chunk 模板选择。
本轮未对这些既有 Rust 路径发现并确认新的实现缺陷；这不是全节点安全证明，
也未关闭完整优化器、准入总预算或分叉头恢复门槛。

## 已复现并修复

| 问题 | 影响 | 修复与反例 |
| --- | --- | --- |
| 续跑的纯审计进程混入 churn RSS | 原本 256/64=2.0 的超限测量，可因重开的 64 lane RSS 较大而变成通过 | 根据尝试起始高度区分 mutation 与 audit-only。相同受控反例在旧 runner 返回 0，修复后返回 1，保留 2.0 比值。 |
| 续跑失败后留下旧矩阵 | 旧成功文件可能被当作当前尝试结果 | 启动时归档旧矩阵和哈希，写运行/失败/中断状态。冻结二进制暂时缺失的真实 CLI 反例产生失败状态且没有当前矩阵，恢复后可续跑。 |
| 重启事件未与观测 PID 对应 | 事件把 old PID 改成未出现过的 999，旧 finalizer 仍接受 | 校验开始前与结束后的附近进程样本、PID 变更、重启模式、时间戳与 duration；旧版误接收反例已保留。 |
| 故障完成可以独立于实际重启 | 不相关的 fault 记录可满足故障门槛 | 必须匹配同网络、相同时间及 PID 的 abrupt restart；拒绝重复事件键，正式七天演练必须在一天后开始。 |
| 空字段被视为有效持久化/追平证据 | 两个空高度可通过相等比较，空 peer-store 也可漏过 | 验证 tip 数字/hash 编码及 mempool/peer-store 非零大小，加入对应拒绝反例。 |

上一轮修复的窗口覆盖、陈旧/乱序/未来样本、未完成/失败演练和零选中测试
校验仍保留。清理后的测试目录增加 retired 标记；新 runner 拒绝续跑，避免
删除数据库后悄悄重新初始化并混入原来的运行身份。

## 验证

- Python runner **9 项测试通过**，包括原始 RSS 失败不能被纯审计重开稀释。
- 公网 monitor、exercise、report 三组 shell 测试通过。
  report 测试还生成覆盖七天的 fixture 验证正式分支；这不是实际公网运行。
- 真实 release 测试二进制完成 20k-live 小规模两通道、compact-copy 五个崩溃
  边界和独立进程重开；续跑保留原始 RSS 比约 **2.006**，按门槛返回 1。
  这是功能/拒绝行为验收，不是 160M 性能结果。
- `cargo clippy --locked --all-targets --all-features -- -D warnings` 通过；
  格式与 diff 检查通过。依赖中已有的 Bitcoin Core C 编译 warning 仍在日志中。
- pinned Core 31 `PostLinearize` **4,096 次精确对照通过**；仅覆盖 refinement，
  不是完整优化器等价性。
- 全特性套件实际执行为 **865 passed / 78 failed / 33 ignored**。
  78 个失败均在 socket 创建/绑定处得到 `Operation not permitted`，与当前沙箱
  禁止网络套接字一致。没有弱化或跳过测试来制造全绿；本轮不能宣称全套通过。
  需要在允许 loopback socket 的环境重跑这些网络测试和实时 Core 差分。

证据位于 `target/audit-2026-09-12/`：原始全量日志与按 target 汇总、严格检查、
两处 before-fix 失败日志、runner 实际 CLI 恢复测试、Core 对照输入输出和
公网 fixture 结果。源码/日志和保留的 release 二进制与测试报告用于复核；
可重建的 debug/coverage/fuzz 编译产物和已完成的一次性测试数据库属于清理范围。
具体删除清单、清理前的分配字节及释放量以该目录的 `cleanup.json` 为准。
此次处理 16 个明确路径，home 卷可用空间增加 37,236,449,280 bytes，
独立 `/tmp` tmpfs 增加 3,387,543,552 bytes，合计约 **37.8 GiB**。
按路径累加的分配量可能重复计算 hard link，所以不把该值冒充实际释放量。
保留 `/data`、冻结 release 二进制、所有报告/源码和 Git 历史；机器专用启动
脚本及其他项目的 `/tmp` 数据未删除。

## 容量与发布限制

`/data` 为独立 NVMe 卷，总容量约 7.0 TiB，可用约 2.9 TiB；完整两通道
412.8 GiB 的规划预检通过。旧“空间不足”只适用于 home 卷。本次既不清理
`/data` 节点数据，也未把它当作主网语料已经齐备的证据。沙箱当前不允许写
`/data`，所以这次只进行了只读预检。

当前 workspace 的 `.git` 被环境设为只读；审计提交在 `/tmp` 中的独立
checkout 创建，保留上述提交链和用户 Git 身份，并导出 bundle 到验收目录。
SSH 默认配置先报权限错误；独立 SSH 配置和 HTTPS 均无法解析 `github.com`。
推送必须记录真实结果，不能把本地 commit/bundle 当作远端已经更新。
